I will add a token usage display to the dashboard.

### **Plan**

1.  **Frontend Update (`client/src/pages/Dashboard.jsx`)**:
    -   Fetch `token_limit_daily` and `tokens_used_today` from the settings API.
    -   Add a new card to the Dashboard grid to display:
        -   **Daily Usage**: `tokens_used_today` / `token_limit_daily`
        -   **Progress Bar**: Visual representation of usage.
        -   **Percentage**: Calculated usage percentage.
    -   Import `PieChart` or `BarChart` icon from `lucide-react` for the card.

### **Implementation Details**
-   The backend already provides this data via `GET /api/settings` (as seen in `server/src/routes.js` and `db.js`), so no backend changes are needed.
-   The new card will sit alongside "Connection Status" and "Bot Control".
