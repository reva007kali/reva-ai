I will add an "Account Settings" page to manage profile details (Name, Email, Logo, Password).

### **Plan**

1.  **Backend Updates**:
    -   **Database Schema**: Update `server/src/db.js` to add `display_name`, `email`, and `logo_path` columns to the `users` table.
    -   **Dependencies**: Install `multer` for handling file uploads (logo).
    -   **API Routes**:
        -   `POST /api/account/update`: Handle multipart form data (profile info + logo upload).
        -   `POST /api/account/password`: Handle secure password updates.
        -   Serve static files from a new `server/uploads` directory so the logo can be displayed.

2.  **Frontend Updates**:
    -   **New Page (`Account.jsx`)**: Create a form to edit:
        -   **Profile**: Display Name, Email, Logo (Image Upload with Preview).
        -   **Security**: Change Password (Current, New, Confirm).
    -   **Navigation**: Add an "Account" link to the `Sidebar`.
    -   **Routing**: Register the new route in `App.jsx`.

3.  **Migration**: Since SQLite doesn't support easy `ALTER TABLE` for multiple columns in one go or complex changes easily without migration scripts in some versions, I will write a safe startup check in `db.js` to add these columns if they are missing.
